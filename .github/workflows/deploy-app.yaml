# name: Deploy React Dashboard on EC2

# on:
#   push:
#     branches: ["main"]

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Use Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "24"

#       - name: Install Dependencies
#         run: npm install --frozen-lockfile
#       # ✅ CRITICAL FIX: Create the .env file BEFORE running the build!
#       - name: Create .env file for Vite
#         run: |
#           cat << 'EOF' > .env
#           ${{ secrets.DASHBOARD_ENVS }}
#           EOF
#       - name: Build React Project
#         run: npm run build

#       # Configure SSH Safely
#       - name: Configure SSH
#         env:
#           SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
#         run: |
#           mkdir -p ~/.ssh
#           cat << 'EOF' > ~/.ssh/id_rsa
#           ${{ secrets.EC2_SSH_PRIVATE_KEY }}
#           EOF
#           chmod 600 ~/.ssh/id_rsa
#           ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

#       # Deploy to EC2
#       - name: Deploy to EC2
#         env:
#           EC2_HOST: ${{ secrets.EC2_HOST }}
#           EC2_USER: ${{ secrets.EC2_USER }}
#         run: |
#           # 1. Create a specific directory for the React dashboard
#           ssh -i ~/.ssh/id_rsa $EC2_USER@$EC2_HOST "mkdir -p ~/amkov-dashboard"


#           # 3. Copy files to EC2 instance
#           rsync -avz -e "ssh -i ~/.ssh/id_rsa" \
#             --exclude='.git' \
#             --exclude='node_modules' \
#             --exclude='.github' \
#             . $EC2_USER@$EC2_HOST:~/amkov-dashboard/

#           # 4. Execute deployment commands safely
#           ssh -i ~/.ssh/id_rsa $EC2_USER@$EC2_HOST << 'EOF'
#             export PATH=$HOME/.local/share/fnm:$PATH
#             eval "`fnm env`"
            
#             # Ensure PM2 and the static 'serve' package are installed globally
#             npm install -g pm2 serve
            
#             cd ~/amkov-dashboard
            
#             # Restart or start the React PM2 process on Port 3001
#             # NOTE: If you used Create React App instead of Vite, change 'dist' to 'build' below!
#             pm2 restart react-dashboard || pm2 start serve --name "react-dashboard" -- -s dist -l 3001
            
#             pm2 save
#           EOF
